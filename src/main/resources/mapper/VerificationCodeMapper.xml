<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.work.work.mapper.VerificationCodeMapper">

    <insert id="insert" parameterType="com.work.work.entity.VerificationCode" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wn_verification_code (target, code, create_time, expire_time, status)
        VALUES (#{target}, #{code}, #{createTime}, #{expireTime}, #{status})
    </insert>

    <select id="getByTargetAndCode" resultType="com.work.work.entity.VerificationCode">
        SELECT * FROM wn_verification_code
        WHERE target = #{target} AND code = #{code}
        ORDER BY create_time DESC LIMIT 1
    </select>

    <select id="getLatestValidCode" resultType="com.work.work.entity.VerificationCode">
        SELECT * FROM wn_verification_code
        WHERE target = #{target}
        AND status = 0
        AND expire_time &gt; NOW()  <!-- 转义 > -->
        ORDER BY create_time DESC LIMIT 1
    </select>

    <update id="markAsUsed">
        UPDATE wn_verification_code SET status = 1 WHERE id = #{id}
    </update>

    <update id="markAsExpired">
        UPDATE wn_verification_code SET status = 2 WHERE id = #{id}
    </update>

    <update id="cleanExpiredCodes">
        UPDATE wn_verification_code SET status = 2
        WHERE status = 0
        AND expire_time &lt;= NOW()  <!-- 转义 <= -->
    </update>
</mapper>